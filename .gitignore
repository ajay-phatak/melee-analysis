# Replay data — large, user-specific
pro_replays/*
!pro_replays/README.md

# Caches
hf_file_list_cache.json
__pycache__/
*.pyc
*.pyo

# Personal scratch notes + analysis outputs (contain connect codes / opponent data)
session*.txt
session.json
trends.txt
backfill.*
# Long-term coach history (source of truth; personal — keep it local / in your vault)
*history*.json

# Python virtual env
.venv/
venv/

# IDE / OS / Claude Code config
.DS_Store
.idea/
.vscode/
.claude/
